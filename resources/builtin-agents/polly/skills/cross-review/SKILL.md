# Cross-review

After implementation, dispatch the reviewer role against each implementation branch. The broker
enforces a provider independent from the coordinator. Request follow-up on the same dispatch when
findings need clarification. Report findings; do not merge.
