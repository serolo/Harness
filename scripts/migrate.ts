// THROWAWAY STUB — Task 4 replaces this with a scratch-DB migration runner
// (`npm run migrate`). Kept minimal so the script target resolves.
console.log('migrate: no migrations wired yet (Task 4).');
