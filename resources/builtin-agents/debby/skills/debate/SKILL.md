# Bounded debate

Dispatch Claude and Codex partners concurrently with identical context. Preserve each response.
For each configured critique round, give each critic the other partner's response. Preserve each
critique and synthesize only after both complete. Never silently replace a missing provider.
